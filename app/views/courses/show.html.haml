!!!
%h1= @course.title
%ul
- if @students.empty?
  %p Course has no registered students
- else
  %h3 Student Roster
  %ul
    - @students.each do |student|
      %li= link_to "#{student.name}", course_path
